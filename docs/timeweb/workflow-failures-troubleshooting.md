# Устранение проблем с GitHub Actions Workflows

Дата: 2025-11-18

## 🔍 Анализ падений

### Падшие workflows

1. **CI #21** - Failed
2. **CodeQL #17** - Failed  
3. **Deploy to Staging #28** - Failed
4. **CodeQL #18** - Success ✅

Все связаны с коммитом `6a94ebd` (docs: add deployment monitoring documentation)

---

## 🔧 Возможные причины

### 1. CI Workflow

**Возможные проблемы:**
- Ошибки линтинга (Biome)
- Ошибки сборки (TypeScript)
- Проблемы с Turborepo кешем
- Отсутствие секретов (TURBO_TOKEN, TURBO_TEAM)

**Проверка:**
```bash
# Локально
pnpm turbo lint
pnpm turbo build
```

### 2. CodeQL Workflow

**Возможные проблемы:**
- Ошибки анализа кода
- Проблемы с автобилдом
- Несовместимость версий

**Проверка:**
- Локально все компилируется успешно
- Возможно, нужна настройка `codeql-config.yml`

### 3. Deploy Workflow

**Возможные проблемы:**
- Ошибки сборки Docker образа
- Проблемы с Trivy сканированием
- Ошибки деплоя в Kubernetes
- Отсутствие секретов (KUBECONFIG, GHCR_TOKEN)

**Проверка:**
```bash
# Локально
docker build -f services/api-gateway/Dockerfile -t test .
```

---

## ✅ Локальная проверка

### Все проверки прошли успешно:

```bash
✅ pnpm lint - успешно
✅ pnpm turbo build - успешно
✅ TypeScript компиляция - успешно
```

---

## 🐛 Рекомендации по устранению

### 1. Проверить логи в GitHub Actions

Откройте каждый failed workflow и проверьте:
- Какая именно команда упала
- Какая ошибка выводится
- На каком шаге произошел сбой

### 2. Проверить секреты

Убедитесь, что все секреты настроены:
- `TURBO_TOKEN`
- `TURBO_TEAM`
- `GHCR_TOKEN`
- `KUBECONFIG`
- `TIMEWEB_API_TOKEN`
- `POSTGRES_URL`
- `REDIS_URL`

### 3. Проверить зависимости

Возможно, нужно обновить `pnpm-lock.yaml`:
```bash
pnpm install
git add pnpm-lock.yaml
git commit -m "chore: update pnpm-lock.yaml"
git push
```

### 4. Проверить CodeQL конфигурацию

Создать `codeql-config.yml` если нужно:
```yaml
name: "CodeQL Config"
paths-ignore:
  - "**/*.test.ts"
  - "**/*.spec.ts"
```

---

## 📝 Следующие шаги

1. ✅ Проверить логи в GitHub Actions
2. ⏳ Исправить найденные ошибки
3. ⏳ Перезапустить workflows
4. ⏳ Проверить успешность деплоя

---

## 🔗 Полезные ссылки

- [GitHub Actions Logs](https://github.com/fow830/maratea/actions)
- [Workflow Files](.github/workflows/)
- [Troubleshooting GitHub Actions](https://docs.github.com/en/actions/using-workflows/troubleshooting-workflows)

