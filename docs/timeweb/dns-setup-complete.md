# Настройка DNS записей - Итоговая инструкция

Дата: 2025-11-18

## 📋 Что нужно сделать

Настроить 2 DNS A записи для домена `betaserver.ru`:

1. `argocd.staging.betaserver.ru` → `62.76.233.233`
2. `grafana.staging.betaserver.ru` → `62.76.233.233`

## 🚀 Быстрый способ

Запустите скрипт для получения инструкций:

```bash
export KUBECONFIG=/path/to/twc-cute-grosbeak-config.yaml
./scripts/setup-dns-timeweb.sh
```

## 📝 Пошаговая инструкция

### Шаг 1: Откройте панель управления Timeweb Cloud

1. Войдите в личный кабинет: https://timeweb.cloud
2. Перейдите в раздел **"Домены"** → **"betaserver.ru"**
3. Откройте раздел **"DNS записи"** или **"Управление DNS"**

### Шаг 2: Создайте первую A запись (ArgoCD)

**Параметры записи:**
- **Имя/Поддомен:** `argocd.staging`
- **Тип:** `A`
- **Значение/IP:** `62.76.233.233`
- **TTL:** `300` секунд

**Как создать:**
1. Нажмите "Добавить запись" или "Создать запись"
2. Выберите тип `A`
3. В поле "Имя" или "Поддомен" введите: `argocd.staging`
4. В поле "Значение" или "IP-адрес" введите: `62.76.233.233`
5. Установите TTL: `300`
6. Сохраните запись

### Шаг 3: Создайте вторую A запись (Grafana)

**Параметры записи:**
- **Имя/Поддомен:** `grafana.staging`
- **Тип:** `A`
- **Значение/IP:** `62.76.233.233`
- **TTL:** `300` секунд

**Как создать:**
1. Нажмите "Добавить запись" или "Создать запись"
2. Выберите тип `A`
3. В поле "Имя" или "Поддомен" введите: `grafana.staging`
4. В поле "Значение" или "IP-адрес" введите: `62.76.233.233`
5. Установите TTL: `300`
6. Сохраните запись

### Шаг 4: Проверка DNS

После сохранения подождите 1-2 минуты и проверьте:

```bash
dig +short argocd.staging.betaserver.ru
# Ожидаемый результат: 62.76.233.233

dig +short grafana.staging.betaserver.ru
# Ожидаемый результат: 62.76.233.233
```

### Шаг 5: Автоматическое получение TLS сертификатов

После того как DNS начнет резолвиться (обычно 1-5 минут), cert-manager автоматически получит TLS сертификаты от Let's Encrypt.

**Проверка сертификатов:**
```bash
export KUBECONFIG=/path/to/twc-cute-grosbeak-config.yaml
kubectl get certificate -A

# Должны увидеть:
# argocd-server-tls    True    argocd-server-tls    argocd
# grafana-tls          True    grafana-tls          observability
```

**Время получения:** 5-10 минут после настройки DNS

## ✅ Финальная проверка

После настройки DNS запустите:

```bash
export KUBECONFIG=/path/to/twc-cute-grosbeak-config.yaml
./scripts/complete-phase1-setup.sh
```

Скрипт проверит:
- ✅ DNS резолюцию
- ✅ TLS сертификаты
- ✅ Доступность сервисов

## 🔍 Что происходит автоматически

1. **DNS распространение** (1-5 минут)
   - DNS записи распространяются по DNS серверам
   - Проверка: `dig +short argocd.staging.betaserver.ru`

2. **Получение TLS сертификатов** (5-10 минут)
   - Cert-manager автоматически запрашивает сертификаты у Let's Encrypt
   - Используется HTTP-01 challenge
   - Проверка: `kubectl get certificate -A`

3. **Доступность сервисов**
   - ArgoCD: `https://argocd.staging.betaserver.ru`
   - Grafana: `https://grafana.staging.betaserver.ru`

## ⚠️ Важные замечания

1. **DNS распространение:** Изменения DNS могут занять до 24 часов для полного распространения, но обычно работают в течение 5-15 минут.

2. **TTL:** Рекомендуется установить TTL на 300 секунд (5 минут) для быстрого обновления при необходимости.

3. **Проверка:** Если DNS не резолвится через 10-15 минут, проверьте:
   - Правильность написания поддомена (argocd.staging, grafana.staging)
   - Правильность IP адреса (62.76.233.233)
   - Сохранение записей в панели управления

4. **TLS сертификаты:** Если сертификаты не получены через 15 минут после настройки DNS, проверьте:
   - DNS резолюцию: `dig +short argocd.staging.betaserver.ru`
   - Логи cert-manager: `kubectl logs -n cert-manager -l app=cert-manager`
   - Challenge ресурсы: `kubectl get challenge -A`

## 📚 Дополнительная документация

- `docs/timeweb/phase1-final-status.md` - Финальный статус Phase 1
- `docs/timeweb/manual-setup-checklist.md` - Полный чек-лист
- `scripts/setup-dns-timeweb.sh` - Скрипт с инструкциями
- `scripts/complete-phase1-setup.sh` - Скрипт проверки статуса

## 🎯 После настройки DNS

После успешной настройки DNS и получения TLS сертификатов:

1. ✅ ArgoCD будет доступен по адресу: `https://argocd.staging.betaserver.ru`
2. ✅ Grafana будет доступен по адресу: `https://grafana.staging.betaserver.ru`
3. ✅ Оба сервиса будут защищены TLS сертификатами от Let's Encrypt
4. ✅ Cert-manager будет автоматически обновлять сертификаты

**Следующие шаги:**
- Настроить S3 Backup (см. `docs/timeweb/s3-backup-setup.md`)
- Настроить Webhook уведомления (см. `docs/timeweb/webhook-setup.md`)

